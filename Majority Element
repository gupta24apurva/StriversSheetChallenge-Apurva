int majorityElement(vector<int>& nums) {
        int n=nums.size(),count=0,ele;
        for(int i=0;i<n;i++)
        {
            if(count==0)
                ele=nums[i];
            
            if(nums[i]==ele)
                count++;
            else
                count--;
        }
        return
